# function name: multiply_numbers
# function arguments: 2, 3, 4
# definition is everything after def and ending with return
# everything below the def line through the return line
# parameters are num1, num2, num3
# invocation is: mutliply_numbers(....)
# return value is stored in the variable 'result'
# identifiers are 'multiply_numbers' and 'product'
def multiply_numbers(num1, num2, num3):
    result = num1 * num2 * num3
    return result

product = multiply_numbers(2, 3, 4)

